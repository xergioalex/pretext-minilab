---
import DemoLayout from '../../layouts/DemoLayout.astro';
import MeasureHeightIsland from '../../islands/demos/MeasureHeight.svelte';
---

<DemoLayout
  title="Basic Paragraph Measurement"
  summary="Prepare once, measure at any width instantly — the core Pretext workflow."
  context="This demo shows the fundamental prepare/layout cycle. Type any text, adjust the width slider, and watch Pretext compute height and line count without touching the DOM. Notice how prepare() is called only when text or font changes, while layout() runs on every width adjustment."
  apis={['prepare()', 'layout()']}
  difficulty="Beginner"
  snippet={`import { prepare, layout } from '@chenglou/pretext';

// 1. Prepare text once (Unicode analysis, font metrics)
const prepared = prepare('Your text here', '16px Inter, sans-serif');

// 2. Layout at any width — pure arithmetic, no DOM
const result = layout(prepared, 400, 24);
console.log(result.height);    // e.g. 72
console.log(result.lineCount); // e.g. 3

// 3. Relayout at a different width — instant
const narrow = layout(prepared, 200, 24);
console.log(narrow.lineCount); // e.g. 6`}
>
  <div class="demo-area" slot="demo">
    <MeasureHeightIsland client:only="svelte" />
  </div>

  <div class="demo-info" slot="info">
    <div class="info-card">
      <h3>What this demonstrates</h3>
      <p>
        The two-phase architecture of Pretext: <code>prepare()</code> does one-time text analysis (Unicode segmentation,
        word boundaries, font measurement), and <code>layout()</code> computes dimensions using pure arithmetic.
        Resizing is the hot path — it should be fast, and it is.
      </p>
    </div>
    <div class="info-card">
      <h3>Relevant Pretext API</h3>
      <ul>
        <li><code>prepare(text, font)</code> — one-time analysis, returns opaque handle</li>
        <li><code>layout(prepared, maxWidth, lineHeight)</code> — returns &#123; height, lineCount &#125;</li>
        <li><code>profilePrepare()</code> — timing breakdown of the prepare step</li>
      </ul>
    </div>
    <div class="info-card">
      <h3>Why this is hard with the DOM</h3>
      <p>
        Traditionally, measuring text height requires rendering it in a hidden element and reading
        <code>offsetHeight</code> — which triggers a layout reflow. Every width change means another reflow.
        With Pretext, the expensive work happens once, and relayout at any width is instant arithmetic.
      </p>
    </div>
  </div>
</DemoLayout>
